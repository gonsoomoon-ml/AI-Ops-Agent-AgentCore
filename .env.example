# =============================================================================
# Ops AI Agent 환경 설정 파일
# =============================================================================
# 사용법: 이 파일을 .env로 복사하고 값을 수정하세요
#   cp .env.example .env
# =============================================================================

# ========== AWS 설정 ==========
AWS_REGION=us-east-1
# AWS_PROFILE=default  # 선택사항: AWS 프로필 지정

# ========== Bedrock 설정 ==========
# 사용 가능한 모델:
#   - global.anthropic.claude-sonnet-4-5-20250929-v1:0 (기본값, 빠름)
#   - global.anthropic.claude-opus-4-5-20251101-v1:0 (고성능)
BEDROCK_MODEL_ID=global.anthropic.claude-sonnet-4-5-20250929-v1:0
BEDROCK_TEMPERATURE=0.0       # 0.0~1.0 (낮을수록 일관된 응답)
BEDROCK_MAX_TOKENS=4096       # 최대 토큰 수 (1~8192)
# BEDROCK_KNOWLEDGE_BASE_ID=your-kb-id-here  # KB ID (create_kb.py 실행 후 입력)

# ========== Datadog 설정 (Phase 2) ==========
# DATADOG_API_KEY=your-api-key-here
# DATADOG_APP_KEY=your-app-key-here
DATADOG_SITE=datadoghq.com

# ========== Agent 설정 ==========
AGENT_LANGUAGE=ko             # ko | en
AGENT_LOG_LEVEL=INFO          # DEBUG | INFO | WARNING | ERROR

# ========== 도구 모드 설정 ==========
# mock: 테스트용 모의 데이터
# mcp: MCP 서버를 통한 실제 API 호출
CLOUDWATCH_MODE=mock          # mock | mcp (MCP: awslabs.cloudwatch-mcp-server)
DATADOG_MODE=mock             # mock | mcp (Phase 2)
# KB_MODE=mock                  # mock: 로컬 YAML 검색 (테스트용)
KB_MODE=mcp                   # mcp: Bedrock KB HYBRID 검색 (운영)

# ========== AgentCore Memory 설정 ==========
AGENTCORE_MEMORY_ENABLED=false
# AGENTCORE_MEMORY_ID=your-memory-id-here
AGENTCORE_SESSION_TTL=3600    # 세션 TTL (초)

# =============================================================================
# Observability 설정 (관측성/모니터링)
# =============================================================================
# Strands (로컬 개발)와 AgentCore (프로덕션 배포) 각각 별도 설정
#
# [Strands 모드 설명]
#   - disabled: 관측성 비활성화
#   - langfuse-public: Langfuse Cloud 사용 (https://cloud.langfuse.com)
#   - langfuse-selfhosted: 자체 호스팅 Langfuse 사용 (VPC 내부)
#
# [AgentCore 모드 설명]
#   - disabled: 관측성 비활성화
#   - langfuse-public: Langfuse Cloud 사용
#   - langfuse-selfhosted: 자체 호스팅 Langfuse 사용
#   - native: AWS 기본 관측성 (ADOT → CloudWatch/X-Ray)
# =============================================================================

# ========== Strands (로컬) Observability ==========
# 로컬 개발 환경에서의 관측성 모드
STRANDS_OBSERVABILITY_MODE=disabled    # disabled | langfuse-public | langfuse-selfhosted

# ========== AgentCore Observability ==========
# AgentCore 런타임 배포 시 관측성 모드
AGENTCORE_OBSERVABILITY_MODE=disabled  # disabled | langfuse-public | langfuse-selfhosted | native

# ========== 공통 설정 ==========
OTEL_SERVICE_NAME=ops-ai-agent         # 서비스 이름 (트레이스에 표시)

# ========== Langfuse Public Cloud 설정 ==========
# langfuse-public 모드 사용 시 필요
# API 키 발급: https://us.cloud.langfuse.com (US) 또는 https://cloud.langfuse.com (EU)
# LANGFUSE_PUBLIC_KEY=pk-lf-xxxxxxxx
# LANGFUSE_SECRET_KEY=sk-lf-xxxxxxxx
LANGFUSE_PUBLIC_ENDPOINT=https://us.cloud.langfuse.com

# ========== Langfuse Self-hosted 설정 ==========
# langfuse-selfhosted 모드 사용 시 필요
# 자체 호스팅 Langfuse 서버의 엔드포인트와 API 키
# LANGFUSE_SELFHOSTED_PUBLIC_KEY=pk-lf-xxxxxxxx
# LANGFUSE_SELFHOSTED_SECRET_KEY=sk-lf-xxxxxxxx
# LANGFUSE_SELFHOSTED_ENDPOINT=http://your-alb.region.elb.amazonaws.com
